%nav{:class=>'navbar navbar-inverse navbar-fixed-top', :role=>'navigation'}
   %div{:class=>"container-fluid"}
      %div{:class=>"navbar-header"}
         %button{"type"=>"button",:class=>"navbar-toggle", "data-toggle"=>"collapse", "data-target"=>"#operationalNavbar"}
            %span{:class=>"icon-bar"}
            %span{:class=>"icon-bar"}
            %span{:class=>"icon-bar"}
         %a{:class=>"navbar-brand", :href=>'/'}Optecture
      %div{:id=>"operationalNavbar", :class=>"collapse navbar-collapse"}
         %ul{:class=>"nav navbar-nav"}
            - if (session[:company_id]!= nil) & (session[:user_id] != nil)
               %li
                  = link_to company_user_path(session[:company_id] ,session[:user_id]) do
                     %span{:class=>"glyphicon glyphicon-home"}
                     Home
            - if (current_user.admin? || current_user.moderator?)
               %li
                  = link_to company_path(session[:company_id] ) do
                     %span CurComp
            -if current_user.admin?
               %li
                  =link_to companies_path do
                     %span AllComp
            - if @showdrawing == true
               %li
                  = link_to newdrawing_company_user_path(session[:company_id], session[:user_id]) do
                     %span{:class=>"fa fa-paint-brush","aria-hidden"=>"true"}
                     New Drawing
            - if @editdetails == true
               %li
                  %a{:href=>'#', :onclick => "onSave( '#{params[:company_id]}', '#{params[:user_id]}', '#{params[:id]}')"}
                     %span{:class=>"fa fa-save","aria-hidden"=>"true"}
                     Save
               %li
                  =link_to bom_company_user_drawing_path(session[:company_id], session[:user_id], params[:id]) do
                     %span{:class=>"fa fa-file-text-o","aria-hidden"=>"true"}
                     BOM
            -if current_user.admin? && !session[:switched] == true
               %li
                  = link_to user_showall_path(session[:user_id]) do
                     %span{:class=>"fa fa-repeat","aria-hidden"=>"true"}
                     Switch User
            -if session[:switched] == true
               %li
                  = link_to company_user_switchback_path(session[:admincompany],session[:adminuser]) do
                     %span{:class=>"fa fa-repeat","aria-hidden"=>"true"}
                     Switch Back
         - if current_user
            %ul{:class=>"nav navbar-nav navbar-right"}
               %li
                  %span{:class=>"navbar-text",:style=>"color:white"}
                     %span{:class=>"fa fa-user","aria-hidden"=>"true"}
                     #{current_user.email}
               %li{:class=>'align-right'}
                  =link_to signout_path, :class => 'last' do
                     %span{:class=>"fa fa-sign-out","aria-hidden"=>"true"}
                     Sign out
